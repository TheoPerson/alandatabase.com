[build]
  command = "pnpm --filter the-alan-s-data-base run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

[functions]
  node_bundler = "esbuild"
  included_files = [".netlify/server/**"]
